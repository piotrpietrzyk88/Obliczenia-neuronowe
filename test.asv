figure(365);
f0=1;
k=10;
a=1:100;

% plot(sin(2*pi*(f0+k/2*a).*a));

t = 0:0.01:2;
t=t(1:200);
y = chirp(t,0,1,3);
plot(y);
% % 
% % f_sample=1e3;
% % f_repetition=3;
% % figure(366);
% % t = 0 : 1/f_sample : 1;         % 1 kHz sample freq for 1 sec 
% % d = 0 : 1/f_repetition : 1;     % 3 Hz repetition freq 
% % y = pulstran(t,d,'rectpuls',2,-5);
% % plot(t,y)
% dlugosc_uczenie=200;
% N=4;
% figure(123);
% r=zeros(1,dlugosc_uczenie);
%     for a=1:dlugosc_uczenie
%         if mod(a,N) == 0 && a>N
%             r(a)=0.5*((rand()*2)-1) + r(a-N);
%         else
%             if a>N
%                 r(a)=r(a-N);
%             else
%                 r(a)=(rand()*2)-1;
%             end
%         end
%     end
%     plot(r)
%     figure(1234);
%     stairs(r)

% for a=1:200
%     u1(a) = sin(a/5);
% end 
% plot(u1)